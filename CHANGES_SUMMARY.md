# خلاصه تغییرات - Graceful Port Binding Error Handling

## مشکل اصلی
هنگامی که سرویسی روی پورت‌های مشخص شده در کانفیگ فعال نباشد (یا اشغال باشد)، برنامه صدای `Fatal` می‌دهد و **فوری متوقف می‌شود**.

## حل پیاده‌سازی شده
تمام توابع `localListener` در تمام transport types را تغییر دادیم:
- **از**: `logger.Fatalf()` → **به**: `logger.Warnf()`
- **نتیجه**: برنامه فقط یک warning ثبت می‌کند و ادامه می‌دهد

## فایل‌های تصحیح شده

### 1. `internal/server/transport/tcp.go`
```go
// قبل:
if err != nil {
    s.logger.Fatalf("failed to listen on %s: %v", localAddr, err)
    return
}

// بعد:
if err != nil {
    s.logger.Warnf("failed to listen on %s: %v", localAddr, err)
    return
}
```

### 2. `internal/server/transport/tcpmux.go`
- همان تغییر برای TCP Multiplexing

### 3. `internal/server/transport/ws.go`
- همان تغییر برای WebSocket
- اصلاح نام متغیر: `listener` → `portListener` (برای تطابق با کد موجود)

### 4. `internal/server/transport/wsmux.go`
- همان تغییر برای WebSocket Multiplexing

### 5. `internal/server/transport/udp.go`
- تغییر دو موقعیت:
  - `net.ResolveUDPAddr()` - خطای resolve آدرس
  - `net.ListenUDP()` - خطای bind به پورت

## رفتار جدید

### قبل (Fatal):
```
[FATAL] failed to listen on :8081: address already in use
[برنامه متوقف می‌شود]
```

### بعد (Warning):
```
[WARNING] failed to listen on :8081: address already in use
[برنامه ادامه می‌دهد و سایر پورت‌ها کار می‌کنند]
```

## مزایا
✅ برنامه بیش‌تر دوام دارد  
✅ سایر پورت‌های صحیح کار می‌کنند  
✅ Log کامل از مشکلات ثبت می‌شود  
✅ معیاری برای Production Environment  

## ملاحظات
- اگر **تمام** پورت‌ها دسترس‌ناپذیر باشند، سرور بدون هیچ listener کار خواهد کرد
- پیشنهاد: Health-check monitoring برای تشخیص چنین حالاتی

## قابلیت جدید بارگذاری ترکیبی
برای ورودی‌هایی که از پورت تکراری استفاده می‌کنند، سیستم اکنون از **الگوریتم بارگذاری ترکیبی** بهره می‌برد: اولین اتصال از هر آی‌پی مبدأ به مقصدی که تا آن لحظه کم‌ترین تعداد اختصاص را داشته هدایت می‌شود (به اصطلاح "کمترین بار"), و برای اتصالات بعدی از همان مبدا، همان مقصد حفظ می‌شود. این رفتار باعث می‌شود تا توزیع اولیه‌ی اتصالات یکنواخت‌تر باشد و در عین حال sticky بودن را نیز داشته باشیم.
- وضعیت داخلی برای هر `dstAddr` نگهداری می‌شود.
- توابع `utils.ResetState(dst)` و `utils.ResetAllStates()` برای پاکسازی دستی وجود دارد.
- یک مکانیزم خودکار نیز افزوده شده است: `utils.StartAutoCleanup(interval,maxAge)` گوروتین‌ پشتی را راه‌اندازی می‌کند که به طور دوره‌ای ورودی‌هایی را که برای بیش از `maxAge` بدون استفاده مانده‌اند، حذف می‌کند. این تابع اکنون از داخل `main` با مقادیر پیش‌فرض فراخوانی می‌شود، بنابراین هیچ اقدام دستی لازم نیست؛ اگر خواستید پارامترها را تغییر دهید یا قبل/بعد از shutdown کنترل بیشتری داشته باشید، می‌توانید مستقیماً آن‌را صدا بزنید و با `StopAutoCleanup()` خاتمه دهید.
مثال پیکربندی:
```toml
ports = [
"16445",
"16445=107.181.134.170:16445",
"16445=45.67.139.215:15346",
"16445=5.253.31.5:4973",
"16445=194.76.147.13:1373",
]
```

این تغییر بدون ایجاد اختلال در قابلیت‌های دیگر برنامه انجام شده و در تمام transport‌ها (TCP, UDP, WS, QUIC و …) اعمال می‌شود.
